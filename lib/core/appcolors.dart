import 'package:flutter/material.dart';

class AppColors {
  // Brand and theme colors
  static const Color primary = Color(0xFF29D177); // main green tone
  static const Color background = Colors.white; // white background
  static const Color banner = Color(0xFF06C25E); // green banner background

  // Text colors
  static const Color primaryText = Colors.black; // main text (black)
  static const Color secondaryText = Color(0xFF474747); // secondary text (gray)

  // UI backgrounds / highlights
  static const Color secondaryBackground = Color(
    0xFFEEEEEE,
  ); // light gray background (cards etc.)

  // Functional colors
  static const Color offer = Color(0xFF8A2BE2); // violet (for offers)
  static const Color notification = Color(
    0xFFF40000,
  ); // red (for notifications)
  static const Color tag = Color(0xFFFFA115); // orange (for tag/minutes)
  static const Color topbg = Color(0xFF41529F); //top
}
